"""spindle — developer news in the Claude Code spinner.

Replaces only Claude Code's thinking verb (e.g. "Meandering…") with a cached
developer-news headline, via the official ``spinnerVerbs`` settings hook.
Everything else Claude renders is left byte-for-byte untouched.
"""

__version__ = "0.1.0"
